default_output: "discord"  # 今回はDiscordのみ
webhook_file: "config/webhooks.env"
category_to_output:
  travel: "discord"
  news:   "discord"
category_to_env:
  travel: "DISCORD_WEBHOOK_TRAVEL"  # 実値は webhooks.env で管理
  news:   "DISCORD_WEBHOOK_NEWS"
rate_limit:
  fetch_sleep_ms: 1200
  post_sleep_ms: 900
filters:
  include_premieres: false
  include_live: false
  include_shorts: true
timezone: "Asia/Tokyo"
